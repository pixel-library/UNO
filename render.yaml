services:
  - type: web
    name: uno-backend
    env: node
    region: singapore
    buildCommand: npm run build:server
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
